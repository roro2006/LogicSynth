# Benchmark inputs

Place Yosys JSON netlists here for offline benchmark runs. Keep public-source
URLs, licenses, checksums, and the exact Yosys command in the published
manifest; generated netlists are intentionally not fetched implicitly by CI.
